<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".RepMateApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RepMate">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.RepMate"
            android:windowSoftInputMode="adjustResize">
            <!--
              TEMPORARY: launcher moved to .SensorProbeActivity for sensor bring-up.
              TO RESTORE: uncomment this intent-filter and delete the .SensorProbeActivity
              entry below (and its Kotlin file). Only one activity should be the launcher,
              otherwise the app installs two icons and Android Studio asks which to start.

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>

        <!--
          ⚠️ TEMPORARY THROWAWAY — DELETE THIS WHOLE <activity> BLOCK, along with
          SensorProbeActivity.kt, before the real workout UI lands. It exists only to verify
          DeviceSensorSource on real hardware. See the KDoc in SensorProbeActivity.kt.
        -->
        <activity
            android:name=".SensorProbeActivity"
            android:exported="true"
            android:label="RepMate sensor probe"
            android:theme="@style/Theme.RepMate">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>